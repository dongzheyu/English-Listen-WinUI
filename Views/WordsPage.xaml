<Page
    x:Class="English_Listen_WinUI.Views.WordsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignHeight="600" d:DesignWidth="800">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="10">
            <Button Content="返回" Click="BackButton_Click" Width="120" Height="40" Margin="10"/>
            <TextBlock Text="单词管理" FontSize="24" FontWeight="Bold" 
                       VerticalAlignment="Center" Margin="20"/>
        </StackPanel>

        <!-- Search and Filter -->
        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="10">
            <TextBlock Text="搜索:" VerticalAlignment="Center" Margin="5"/>
            <TextBox x:Name="SearchTextBox" Width="200" 
                      TextChanged="SearchTextBox_TextChanged"
                      PlaceholderText="输入单词进行搜索"/>
            <TextBlock Text="筛选:" VerticalAlignment="Center" Margin="10,0,5,0"/>
            <ComboBox x:Name="FilterComboBox" Width="150"
                      SelectionChanged="FilterComboBox_SelectionChanged">
                <ComboBoxItem Content="全部单词" IsSelected="True"/>
                <ComboBoxItem Content="以字母开头"/>
                <ComboBoxItem Content="以数字开头"/>
            </ComboBox>
            <Button Content="重置" Click="ResetFilterButton_Click" Margin="10"/>
        </StackPanel>

        <!-- Word List -->
        <Grid Grid.Row="2" Margin="10">
            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <ScrollViewer Grid.Row="0" VerticalScrollBarVisibility="Auto" Margin="10">
                <TextBox x:Name="WordsTextBox" 
                          AcceptsReturn="True" 
                          TextWrapping="Wrap"
                          FontSize="14"
                          MinHeight="300"
                          TextChanged="WordsTextBox_TextChanged"/>
            </ScrollViewer>

            <!-- Add Word -->
            <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="10" HorizontalAlignment="Center">
                <TextBox x:Name="WordInput" Width="150" 
                          KeyDown="WordInput_KeyDown"
                          PlaceholderText="输入单词"/>
                <Button Content="添加单词" Click="AddWordButton_Click" Width="110" Height="35" Margin="10"/>
                <Button Content="删除选中" Click="DeleteSelectedButton_Click" Width="110" Height="35" Margin="10"/>
                <Button Content="清空列表" Click="ClearButton_Click" Width="110" Height="35" Margin="10"/>
                <Button Content="导入词库" Click="ImportButton_Click" Width="110" Height="35" Margin="10"/>
                <Button Content="导出词库" Click="ExportButton_Click" Width="110" Height="35" Margin="10"/>
            </StackPanel>
        </Grid>

        <!-- Footer Buttons -->
        <StackPanel Grid.Row="3" Orientation="Horizontal" 
                    HorizontalAlignment="Center" Margin="20">
        </StackPanel>
    </Grid>
</Page>
